server:
  port: 8800
spring:
  servlet:
    multipart:
      max-file-size: 50MB
      max-request-size: 50MB
  application:
    name: shiyiblog
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/blog?characterEncoding=UTF-8&useUnicode=true&useSSL=false&serverTimezone=Asia/Shanghai&tinyInt1isBit=false
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: root
    type: com.alibaba.druid.pool.DruidDataSource
    initial-size: 10
    max-active: 150
    min-idle: 10
    max-wait: 5000
    pool-prepared-statements: false
    validation-query: SELECT 1
    validation-query-timeout: 500
    test-on-borrow: false
    test-on-return: false
    test-while-idle: true
    time-between-eviction-runs-millis: 60000
    min-evictable-idle-time-millis: 30000
    filters: stat,wall,log4j
    connectionProperties: druid.stat.mergeSql=true;druid.stat.slowSqlMillis=5000
  elasticsearch:
    rest:
      uris: localhost:9200
  redis:
    host: 127.0.0.1
    port: 6379
    timeout: 3000
    password:
  mail:
    # 配置 SMTP 服务器地址
    host: smtp.qq.com
    # 发送者邮箱
    username: 3085755420@qq.com
    # 配置密码，注意不是真正的密码，而是刚刚申请到的授权码
    password: dkyxbhngmdwydfbf
    # 端口号465或587
    port: 587
    # 默认的邮件编码为UTF-8
    default-encoding: UTF-8
    # 配置SSL 加密工厂
    properties: # 设置邮件超时时间防止服务器阻塞
      timeout: 5000
      connection-timeout: 5000
      write-timeout: 5000
      mail:
        smtp:
          socketFactoryClass: javax.net.ssl.SSLSocketFactory
        #表示开启 DEBUG 模式，这样，邮件发送过程的日志会在控制台打印出来，方便排查错误
        debug: true

#==============================IP限流次数和时间配置==============================
IpLimit:
  #请求次数
  count: 10
  #请求时间
  time: 10

#==============================线程池配置==============================
task:
  executor:
    core_pool_size: 10
    max_pool_size: 200
    queue_capacity: 1200
    keep_alive_seconds: 60

#==============================Sa-Token配置==============================
sa-token:
  # token名称 (同时也是cookie名称)
  token-name: Authorization
  # token有效期，单位s 默认30天, -1代表永不过期
  timeout: 3600
  # token临时有效期 (指定时间内无操作就视为token过期) 单位: 秒
  activity-timeout: -1
  # 是否允许同一账号并发登录 (为true时允许一起登录, 为false时新登录挤掉旧登录)
  is-concurrent: true
  # 在多人登录同一账号时，是否共用一个token (为true时所有登录共用一个token, 为false时每次登录新建一个token)
  is-share: false
  # token风格
  token-style: random-128
  # 是否输出操作日志
  is-log: false

#==============================mybatis-plus配置==============================
mybatis-plus:
  mapper-locations: classpath:/mapper/*Mapper.xml
  #实体扫描，多个package用逗号或者分号分隔
  typeAliasesPackage: com.shiyi.entity
  global-config:
    # 数据库相关配置
    db-config:
      #主键类型  AUTO:"数据库ID自增", INPUT:"用户输入ID",ID_WORKER:"全局唯一ID (数字类型唯一ID)", UUID:"全局唯一ID UUID";
      id-type: AUTO
      #字段策略 IGNORED:"忽略判断",NOT_NULL:"非 NULL 判断"),NOT_EMPTY:"非空判断"
      field-strategy: not_empty
      #驼峰下划线转换
      column-underline: true
      db-type: mysql
    #刷新mapper 调试神器
    refresh: true
  # 原生配置
  configuration:
    map-underscore-to-camel-case: true
    cache-enabled: false
  logging:
    level: warn

#==============================knife4j配置=======================================
knife4j:
  # 开启增强配置
  enable: true
  basic:
    enable: true
    # Basic认证用户名
    username: admin
    # Basic认证密码
    password: admin

#============================第三方登录配置信息===================================
qq:
  app-id: 101995894
  check-token-url: https://graph.qq.com/oauth2.0/me?access_token={access_token}
  user-info-url: https://graph.qq.com/user/get_user_info?openid={openid}&access_token={access_token}&oauth_consumer_key={oauth_consumer_key}
weibo:
  app-id: 2285022919
  app-secret: a380fef0743e38cad12125902b204fab
  grant-type: authorization_code
  redirect-url: http://www.shiyit.com/callback/weibo
  access-token-url: https://api.weibo.com/oauth2/access_token
  user-info-url: https://api.weibo.com/2/users/show.json?uid={uid}&access_token={access_token}
gitee:
  app-id: d029020a6839273e0e82d853783874bf1f4f34b95da6ebcbc230e6603ff9b656
  app-secret: e94dfa5baaa5ac896bd01d077d742020504910a0a4c974a83d40d51a0d8377b8
  grant-type: authorization_code
  redirect-url: http://www.shiyit.com/callback/gitee
  access-token-url: https://gitee.com/oauth/token
  user-info-url: https://gitee.com/api/v5/user?access_token={access_token}
baidu:
  url: http://data.zz.baidu.com/urls?site=www.shiyit.com&token=aw5iVpNEB9aQJOYZ
file:
  upload-folder: D:/jpg/

#==============================DeepSeek AI配置==============================
deepseek:
  # DeepSeek API密钥（请替换为您的实际API密钥）
  api-key: sk-2544e86767664253a170bc1833eb3100
  # API基础URL
  base-url: https://api.deepseek.com
  # 聊天接口路径
  chat-path: /v1/chat/completions
  # 模型名称
  model: deepseek-chat
  # 系统提示词（用于推送精品内容）
  system-prompt: 你是一个专业的博客内容推荐助手，擅长根据用户需求推荐精品文章内容。请用简洁、友好的语言与用户交流，并推荐相关的优质内容。
  # 温度参数（0-2，控制回复的随机性，值越大越随机）
  temperature: 0.7
  # 最大token数
  max-tokens: 2048
  # 连接超时时间（秒）
  connect-timeout: 10
  # 读取超时时间（秒）
  read-timeout: 60